'use strict';

/* Directives */

angular.module('directives', []);
    /*.directive('makebuttonset',function() {
        return function(scope, elm, attrs) {
            //elm.makebuttonset();
        };
    })*/